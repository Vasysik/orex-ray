# OrexRay 0.5.1 hotfix

Исправляет конфликт имени `AboutScreen.build`:

- `static const build` переименован в `static const buildNumber`;
- обновлены ссылки в тексте версии и в буфере обмена.

Применить поверх OrexRay 0.5.0.
